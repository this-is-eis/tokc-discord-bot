name = "tokc-discord-bot"
main = "src/index.js"
compatibility_date = "2024-01-01"

# Required for Ed25519 signature verification
# Reference: https://developers.cloudflare.com/workers/configuration/compatibility-dates/#nodejs-compatibility-flag
compatibility_flags = ["nodejs_compat"]

[vars]
# Non-sensitive config can go here
# CARDS_JSON_URL = "https://your-domain.com/cards.json"

# Secrets (set via `wrangler secret put`):
# - DISCORD_PUBLIC_KEY
# - DISCORD_APPLICATION_ID (optional, for command registration)
# - DISCORD_BOT_TOKEN (optional, for command registration)
